// the page will hide paragraph//
// and then show paragraph slowly //
// The code in this document,
// can also be written like this:
// var myCode = function() {}
// jQuery(".yummy").hide();
// jQuery(".yummy").show("slow");
// $(document).ready(myCode);
// but because we've included the
// jQuery documentation at the bottom
// of the html page, we can skip
// the naming a function part.
// The code as written is best practice.


$(".yummy").hide().show("slow");
